/*
ESP will make a post req to server 
this will make sure only known devices in the DB can 
psuh data the server
(only our devices can push)
(can add new devices w/0 manual DB setup)
*/

import jwt from 'jsonwebtoken';
impoe
